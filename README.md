# Setup project 

you do need install:

```pip install fastapi```

```pip install uvicorn```

```pip install 'python-jose[cryptography]' 'passlib[bcrypt]'```

# Run code

```cd app/``

```uvicorn app:app --reload```
# fast-api
