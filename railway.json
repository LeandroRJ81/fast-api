{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "NIXPACKS"
    },
    "deploy": {
      "startCommand": "app && hypercorn main:app --bind \"[::]:$PORT\""
    }
  }